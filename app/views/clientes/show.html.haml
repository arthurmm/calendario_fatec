.cliente.show
    %p.detalhe
        %h4=  @cliente.nome
        %p
            %p
                %strong RG do cliente:
                %label= @cliente.rg
            %p
                %strong Data de Nascimento:
                %label= @cliente.data_de_nascimento

    %h4 Contatos
    .contatos
        %table
            %tr
                %th Forma de Contato
                %th Exluir
            -@cliente.contatos.each do |contato|
                %tr
                    %td=contato.valor
                    %td=link_to "apagar", cliente_contato_path(contato), :confirm => "Quer deletar essa merda?", :method => :delete


-content_for :submenu do
    %ul
        %li= link_to "Adicionar Contato", new_cliente_contato_path(@cliente)
        %li= link_to "Adicionar OS"
        %li= link_to "Voltar", clientes_path

