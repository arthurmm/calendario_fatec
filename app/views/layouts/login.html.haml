!!!
%html
%head
    %title Aps3
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
%body
    .container
        #header
            %h1 Martelinho de prata
            %h2 sua melhor solução em ordem de serviço

        #menu

        #content
            #content_top
            #content_main
                = yield
            #content_bottom
            #footer= debug(params) if Rails.env.development?

