!!!
%html
%head
    %title Aps3
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
%body
    .container
        #header
            %h1 Martelinho de prata
            %h2 sua melhor solução em ordem de serviço
            .right= "Bem Vindo #{current_user.name}"

        #menu
            =render :partial => "layouts/menu"
        #leftmenu
            #leftmenu_top
            #leftmenu_main
                %h3 Sub-menu
                =render :partial => 'submenu' rescue nil
            #leftmenu_bottom

        #content
            #content_top
            #content_main
                = yield
            #content_bottom
            #footer

